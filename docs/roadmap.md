# Deprecated: roadmap removed for minimal bot reset.

1. Create interfaces and a simple in‑memory RxDB collection.
2. Wire message events to the buffer.
3. Add a single stats command that reads totals.
